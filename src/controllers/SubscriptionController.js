const User = require("../models/User");

const { cpf: cpfEval } = require("essential-validation");

module.exports = {
  async index(req, res) {
    const { user } = req;
  },
  async show(req, res) {
    const { user } = req;
  },
  async store(req, res) {
    const { user } = req;
  },
  async update(req, res) {
    const { user } = req;
  },
};
